{% extends 'SquazicHighlineGuideBundle::admin.html.twig' %}

{% block content %}
<h1>Artwork creation</h1>

<form action="{{ path('artwork_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('artwork') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
